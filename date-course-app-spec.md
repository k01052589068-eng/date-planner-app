# 데이트 코스 추천 앱 — 개발 명세서 (Claude Code용)

> 가칭: **이번주 데이트**
> 작성일: 2026-09-23
> 이 문서는 Claude Code가 처음부터 끝까지 구현할 수 있도록 작성된 명세서다. 구현 중 명세와 다른 판단이 필요하면 **실행 전에 옵션을 제안하고 확인받을 것.** 사용자가 직접 해야 하는 작업(키 발급, 콘솔 설정 등)이 생기면 **미리 알려줄 것.**

---

## 1. 앱 개요

커플이 함께 쓰는 데이트 코스 추천 + 데이트 다이어리 PWA.

- 매주 월요일, 이번 주에 할 만한 국내 데이트 코스를 자동 추천한다.
- 추천은 커플이 설정한 **위치·취향**을 반영하되, 설정한 **새로움 빈도**에 따라 가끔 위치·취향 밖의 코스도 섞는다.
- 원하는 **장소·기간**을 지정해 그때 할 수 있는 데이트를 검색할 수 있다.
- 다녀온 데이트는 다이어리로 기록하고 **시간순** / **지도(장소별)** 로 본다.
- 누구나 가입해 쓸 수 있는 공개 서비스다.

---

## 2. 확정된 결정 사항

| 항목 | 결정 |
|---|---|
| 사용 단위 | 커플 1쌍이 하나의 공간을 공유 (초대 코드로 연결) |
| 운영 예산 | **완전 무료 범위만** — 카드 등록이 필요한 서비스는 사용하지 않음 |
| 플랫폼 | 모바일 우선 PWA (Netlify 호스팅 + Firebase 무료 요금제) |
| 추천 생성 방식 | **A + C 조합** — A: 공공데이터 + 규칙 기반 자동 생성 / C: 사용자가 주 1회 Claude Code로 트렌드 조사 실행(선택) |
| 주간 자동 실행 | GitHub Actions 예약 실행 (무료, 카드 불필요) |
| 다이어리 사진 | **사용하지 않음** (텍스트·별점 중심 기록) |
| 지도 | 카카오맵 JavaScript SDK |
| 제외 | Claude/OpenAI 유료 API, Firebase Storage·Cloud Functions(Blaze 필요), 네이버 검색 API(계정 이용 제한 이슈), 인스타그램 API·스크래핑 |

---

## 3. 기술 스택과 아키텍처

```
[사용자 폰 (PWA)]
   │  Google 로그인 / 데이터 읽기·쓰기
   ▼
[Firebase — Spark(무료)]
   ├─ Authentication (Google)
   └─ Firestore (커플, 설정, 코스 풀, 다이어리)
   ▲
   │  주 1회 코스 풀 업로드 (Admin SDK)
[GitHub Actions — 매주 월요일 00:00 KST]
   ├─ A 파이프라인: TourAPI 조회 → 규칙 기반 코스 생성
   └─ C 결과 반영: data/trends/*.json 이 push되면 업로드
   ▲
   │  git push
[사용자 PC — Claude Code]
   └─ /weekly-trends 명령 (C 파이프라인, 선택 실행)

[Netlify] ← GitHub 저장소 연결, 프런트엔드 자동 빌드·배포
```

- **프런트엔드**: Vite + React, PWA(manifest + service worker). 모바일 세로 화면 기준 설계.
- **백엔드 서버 없음.** 무거운 작업은 전부 GitHub Actions에서 주 1회 처리하고, 앱은 Firestore만 읽는다.
- **저장소 구조(권장)**

```
/web                 # 프런트엔드 (Vite + React)
/scripts             # Node.js 파이프라인 (TourAPI 수집, 코스 생성, 업로드)
/data/trends         # C 파이프라인 결과 JSON (주차별)
/data/templates      # 계절·테마 코스 템플릿
/.claude/commands    # Claude Code 사용자 명령 (weekly-trends.md)
/.github/workflows   # weekly.yml, trends-upload.yml
firestore.rules
```

### 무료 한도 관련 설계 원칙
- Firestore 무료 한도(일 읽기 5만 / 쓰기 2만 / 저장 1GiB)를 넘지 않도록 **코스 풀은 "시·도 × 주차" 단위로 한 문서에 배열로 묶어** 저장한다. 사용자 한 명이 이번 주 추천을 보는 데 읽기 몇 회면 충분해야 한다.
- 개인화 추천 계산은 **클라이언트에서** 한다(서버 호출 없음). 결과는 커플 문서에 캐시한다.
- TourAPI 키는 **GitHub Actions Secrets에만** 두고 클라이언트에 노출하지 않는다.

---

## 4. 사용자(강빈)가 준비할 것

구현 단계에 맞춰 Claude Code가 안내하되, 미리 알아둘 목록:

- [ ] **GitHub 계정** + 새 저장소 생성
- [ ] **공공데이터포털(data.go.kr)** 가입 → "한국관광공사_국문 관광정보 서비스_GW" 활용 신청 → 서비스 키 발급 (승인까지 시간이 걸릴 수 있음)
- [ ] **Firebase 프로젝트** 생성 (Spark 요금제 유지) → Authentication에서 Google 로그인 활성화 → Firestore 생성 → 서비스 계정 키(JSON) 발급
- [ ] **카카오 디벨로퍼스** JavaScript 키 (기존 앱 키 재사용 가능) → 플랫폼에 Netlify 도메인 등록
- [ ] **Netlify**에 GitHub 저장소 연결
- [ ] GitHub 저장소 Secrets 등록: `TOURAPI_KEY`, `FIREBASE_SERVICE_ACCOUNT`
- [ ] Firebase 승인된 도메인에 Netlify 도메인 추가
- [ ] (C안) **매주 1회** PC에서 Claude Code 실행 → `/weekly-trends` → 결과 확인 후 push (약 5분)

---

## 5. 기능 명세

### 5-1. 로그인과 커플 연결
- Google 로그인만 지원. 로그인 방식은 모바일 브라우저 호환을 고려해 선택하고, 팝업이 막히는 환경에 대한 대비 로직을 넣는다.
- 첫 로그인 후 두 가지 선택지:
  - **새 커플 공간 만들기** → 6자리 초대 코드 생성 (24시간 유효, 1회용)
  - **초대 코드 입력** → 상대 공간에 합류
- 한 커플 공간은 최대 2명. 설정·추천·다이어리는 모두 커플 공간 단위로 공유되고 실시간 동기화된다.
- 설정 화면에서 연결 해제 가능 (해제 시 데이터 처리 방식은 §9 미결 사항 참고).

### 5-2. 기본 설정 (커플 공유)
| 설정 | 내용 |
|---|---|
| 기준 위치 | 카카오 장소 검색으로 지정 (예: 송도). 좌표 저장 |
| 이동 반경 | 10km / 30km / 50km / 100km / 전국 |
| 취향 카테고리 (복수 선택) | 맛집·카페 / 전시·공연 / 액티비티·체험 / 자연·산책 / 축제·시즌 이벤트 / 실내(비 오는 날) / 드라이브 |
| 새로움 빈도 | 안 함 / 가끔(4주에 1회) / 보통(2주에 1회) / 자주(매주) |
| 주간 추천 개수 | 기본 5개 (3~7 조절) |

- "새로움" 코스 = 이동 반경 밖 지역 **또는** 선택하지 않은 취향 카테고리의 코스. 추천 카드에 "새로운 시도" 배지를 붙인다.

### 5-3. 주간 추천 (핵심)

#### A 파이프라인 — 자동 (GitHub Actions, 매주 월요일 00:00 KST)
- cron: `0 15 * * 0` (UTC 일요일 15:00 = KST 월요일 00:00). 수동 실행(`workflow_dispatch`)도 지원.
- TourAPI 4.0 (`https://apis.data.go.kr/B551011/KorService2`) 사용:
  - `searchFestival2`: 이번 주(월~일)와 기간이 겹치는 축제·행사 → **비정기 데이트**
  - `areaBasedList2` / `areaBasedSyncList2`: 관광지(12), 문화시설(14), 레포츠(28), 음식점(39) 등 → **정기 데이트** 재료. 관광지 데이터는 월 1회 전체 갱신 + 주간 증분 갱신으로 캐시
  - `searchKeyword2`: 계절 키워드(벚꽃, 야경, 단풍, 일루미네이션 등) 보조 검색
- **코스 조합 규칙** (`/data/templates`):
  - 코스 = 메인 장소 1곳(행사·관광지·전시) + 주변(반경 약 2km) 식사/카페 1곳 + 선택 1곳
  - 계절 템플릿: 월별로 어울리는 테마·키워드 정의 (예: 10월 → 단풍 산책, 가을 축제, 야외 공연)
  - 날짜가 있는 행사는 기간을 코스에 표시
- 결과를 `coursePool/{weekId}_{sido}` 문서로 업로드. 지난 주차는 4주 후 자동 삭제.

#### C 파이프라인 — 반자동 (Claude Code, 선택)
- 저장소에 `.claude/commands/weekly-trends.md` 사용자 명령을 만든다. 명령이 할 일:
  1. 이번 주차·계절을 계산
  2. 웹 검색으로 이번 주 국내 데이트 트렌드 조사 (팝업스토어, 신규 전시, 시즌 명소, 화제의 축제, 블로그·뉴스에서 언급이 많은 곳). 권역별(수도권, 강원, 충청, 전라, 경상, 제주)로 고르게
  3. 각 항목의 **날짜·장소·운영 여부를 출처 URL로 교차 확인**, 확인 안 되면 제외
  4. 아래 스키마로 `data/trends/{weekId}.json` 저장 후 요약을 보여주고, 사용자 확인 후 commit·push
- push되면 `trends-upload.yml`이 JSON을 검증하고 코스 풀에 병합 (`source: "C"`).
- C를 실행하지 않은 주에도 A만으로 추천이 정상 동작해야 한다.

#### 코스 데이터 스키마 (A·C 공통)
```json
{
  "id": "2026-W39-seoul-001",
  "weekId": "2026-W39",
  "source": "A",
  "kind": "irregular",
  "title": "성수 팝업 + 서울숲 산책",
  "summary": "한 줄 설명",
  "themes": ["전시·공연", "자연·산책"],
  "sido": "서울", "sigungu": "성동구",
  "lat": 37.54, "lng": 127.04,
  "period": { "start": "2026-09-21", "end": "2026-10-05" },
  "stops": [
    { "order": 1, "name": "", "type": "event", "lat": 0, "lng": 0, "addr": "", "contentId": "", "url": "" }
  ],
  "sources": ["https://..."],
  "seasonNote": "가을 한정"
}
```
- `kind`: `regular`(상시 가능) / `irregular`(기간 한정)

#### 개인화 선택 (클라이언트)
- 이번 주 코스 풀에서 점수를 매겨 N개 선택:
  - 기준 위치와의 거리(반경 내 가점), 취향 카테고리 일치, 이번 주 한정(`irregular`) 가점, C 출처 가점
  - 최근 4주 내 추천된 코스와 다이어리에 기록된 장소는 제외
- 새로움 빈도에 해당하는 주에는 N개 중 1개를 반경 밖 또는 미선택 취향에서 고른다.
- **난수 시드 = 커플ID + 주차** → 두 사람이 따로 열어도 같은 결과. 결과는 `couples/{id}/weeklyRecs/{weekId}`에 캐시하고, 이전 주차 추천도 다시 볼 수 있게 한다.
- 추천 카드: 제목, 지역, 기간, 코스 경로(지도 미니맵), 배지(이번 주만 / 새로운 시도 / 트렌드), "카카오맵에서 보기", **"다녀왔어요"** 버튼(다이어리 작성 화면으로, 내용 미리 채움).

### 5-4. 장소·기간 검색
- 입력: 장소(카카오 장소 검색 또는 시·도 선택) + 기간(시작~종료일) + 취향 필터(선택)
- 검색 대상:
  1. 저장된 코스 풀 (현재 주차 기준 최근 4주)
  2. GitHub Actions가 주 1회 미리 수집해 둔 **향후 90일 전국 축제·행사 캐시** (`events/{sido}` 문서) — 기간이 겹치는 행사
  3. 캐시된 관광지 데이터로 그 지역의 상시 코스 즉석 조합 (5-3의 조합 규칙 재사용)
- 결과를 "이 기간에만" / "언제든 가능"으로 나눠 보여준다.
- 모든 조회는 Firestore 캐시에서 처리 (클라이언트가 TourAPI를 직접 호출하지 않음).

### 5-5. 데이트 다이어리
- 입력 항목: 날짜(필수), 제목, 장소(카카오 장소 검색 → 이름·좌표 저장, 여러 곳 가능), 한 줄 메모, 별점(1~5), 비용(선택), 연결된 추천 코스 ID(자동), 작성자
- **시간순 보기**: 월별로 묶인 타임라인, 최신순/오래된순 전환
- **장소별 보기**:
  - 카카오맵 전체 화면에 다녀온 장소를 핀으로 표시, 가까운 핀은 클러스터로 묶음
  - 핀 클릭 → 하단 시트에 그 장소의 다이어리 목록(날짜·제목·별점) → 항목 클릭 시 상세
  - 첫 화면은 모든 핀이 보이도록 자동 확대·축소
- 수정·삭제는 커플 구성원 둘 다 가능

### 5-6. 공개 서비스 요건
- 누구나 Google 로그인으로 가입 가능.
- Firestore 보안 규칙:
  - `couples/**`: 해당 커플 구성원만 읽기·쓰기
  - `coursePool/**`, `events/**`: 로그인 사용자 읽기 전용, 쓰기는 Admin SDK(GitHub Actions)만
  - 초대 코드 문서는 코드 소유자만 생성, 합류 시 1회 사용 처리
- 위치 정보를 다루므로 간단한 **개인정보 처리방침 페이지**를 둔다.
- 카카오 키는 등록 도메인 제한으로 보호.

---

## 6. 화면 구성

하단 탭 4개:

1. **이번 주** — 이번 주 추천 카드 목록, 지난주 추천 보기
2. **찾기** — 장소·기간 검색
3. **다이어리** — 상단 토글: 시간순 / 지도
4. **설정** — 기준 위치, 반경, 취향, 새로움 빈도, 추천 개수, 커플 연결 정보, 로그아웃

디자인은 별도 단계에서 정한다(참고 앱을 받은 뒤 결정). 초기에는 깔끔한 기본 스타일로 구현.

---

## 7. 개발 단계

| 단계 | 내용 | 사용자 작업 |
|---|---|---|
| 0 | 저장소 구조, Vite + React + PWA 기본 세팅, Netlify 배포 확인 | GitHub·Netlify 연결 |
| 1 | Firebase 연동, Google 로그인, 커플 생성·초대 코드 합류, 보안 규칙 | Firebase 프로젝트 생성 |
| 2 | 설정 화면 (카카오 장소 검색 포함) | 카카오 도메인 등록 |
| 3 | A 파이프라인 스크립트 (TourAPI 수집 → 코스 조합 → 업로드), 로컬 실행으로 검증 | TourAPI 키 발급 |
| 4 | GitHub Actions 주간 실행 연결 | Secrets 등록 |
| 5 | 이번 주 탭 (개인화 선택, 추천 카드, 캐시) | — |
| 6 | 찾기 탭 (행사 캐시, 즉석 조합) | — |
| 7 | 다이어리 (작성, 시간순, 지도 보기) | — |
| 8 | C 파이프라인 (`/weekly-trends` 명령 + 업로드 워크플로) | 첫 실행 함께 테스트 |
| 9 | PWA 마감(설치, 오프라인 기본 화면), 개인정보 처리방침, 실사용 테스트 | 두 사람이 폰에 설치해 테스트 |
| 10 | 추가 기능 (§8에서 선택된 것만) | 기능 선택 |

각 단계가 끝날 때마다 배포해서 폰으로 확인할 수 있는 상태를 유지한다.

---

## 8. 추가 기능 후보 (미확정 — 사용자가 최종 선택)

선택한 항목만 [x] 표시 후 구현한다. 모두 무료 범위에서 가능한 것만 골랐다.

- [X] **날씨 연동** — 기상청 단기예보(공공데이터, 무료)로 비 예보가 있는 날은 실내 코스를 우선 추천
- [ ] **교대근무 휴무일 자동 계산** — 4조2교대 근무 패턴(조, 기준일)을 입력하면 두 사람의 공통 휴무일을 계산해 그 날짜 기준으로 추천·행사 필터
- [X] **중간 지점 추천** — 두 사람의 집 위치를 각각 넣으면 중간 지점 기준으로 거리 계산
- [X] **공동 찜 목록** — 추천 코스나 검색 결과를 "가고 싶어요"로 저장, 둘 다 찜하면 표시
- [X] **링크 붙여넣기 저장** — 인스타·릴스·블로그에서 본 곳의 링크와 장소를 찜 목록에 추가 (SNS API 대체 수단)
- [X] **좋아요/별로 피드백** — 추천 카드와 다이어리 별점을 다음 추천 점수에 반영
- [X] **예상 비용·예산 필터** — 코스별 대략적인 비용대 표시, 예산 이하만 보기
- [X] **이동 정보** — 코스 내 장소 간 거리 표시, 카카오맵 길찾기 바로 열기
- [X] **기념일 D-day** — 사귄 날 입력 → 100일·주년 표시, 다가오는 기념일에 특별 코스 추천
- [X] **연말 리캡** — 올해 다녀온 곳 수, 지역 분포, 가장 높은 별점 데이트 등을 한 화면에 요약

---

## 9. 구현 제약과 대안 정리

| 원래 요구 | 제약 | 채택한 대안 |
|---|---|---|
| SNS 트렌드 수집 | 인스타 API는 주당 해시태그 30개 제한 + 앱 심사 필요, 스크래핑은 약관 위반 | TourAPI 행사 데이터 + C 파이프라인(웹 검색 기반 트렌드 조사) + 링크 붙여넣기(후보) |
| AI 자동 추천 | 유료 API는 예산 밖 | 규칙 기반 자동 생성(A) + 구독 내 Claude Code 반자동 조사(C) |
| 매주 자동 업데이트 | Firebase 예약 함수는 카드 등록(Blaze) 필요 | GitHub Actions 예약 실행 |
| 사진 첨부 | Firebase Storage는 카드 등록(Blaze) 필요 | 사진 없이 기록 (추후 필요 시 재검토) |
| 네이버 검색 데이터 | 네이버 개발자센터 계정 이용 제한 | 사용 안 함. 제한이 풀리면 C 파이프라인 보조 소스로 추가 검토 |
| 실시간 장소·기간 검색 | 클라이언트에서 TourAPI 호출 시 키 노출 | 주 1회 향후 90일 행사·관광지 캐시를 Firestore에 저장해 검색 |

### 미결 사항 (구현 중 해당 단계에서 확인)
- 커플 연결 해제 시 다이어리 처리 (둘 다 사본 보관 / 삭제 / 30일 보관 후 삭제)
- 앱 정식 이름과 아이콘
- 디자인 참고 앱
