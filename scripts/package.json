{
  "name": "date-planner-scripts",
  "private": true,
  "type": "module",
  "scripts": {
    "weekly": "node --env-file-if-exists=.env weekly.js",
    "trends": "node --env-file-if-exists=.env trends.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "firebase-admin": "^14.5.0"
  }
}
